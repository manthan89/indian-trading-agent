{
  "framework": "nextjs",
  "buildCommand": "cd frontend && npm run build && rm -rf ../.next && ln -sfn $(pwd)/.next ../.next",
  "outputDirectory": ".next",
  "installCommand": "cd frontend && npm install"
}
